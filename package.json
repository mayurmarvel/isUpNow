{
  "name": "isupnow",
  "version": "1.0.0",
  "description": "A Simple SERVERLESS Microservice that checks whether a a website is up or not.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "netlify-lambda": "^2.0.16",
    "node-fetch": "^2.6.9"
  }
}
